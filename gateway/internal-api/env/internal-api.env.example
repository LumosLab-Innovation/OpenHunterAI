NODE_ENV=development
SERVICE_NAME=internal-api
INTERNAL_API_PORT=4100
DATABASE_URL=postgresql://xhunter:xhunter@localhost:5432/xhunter?schema=public
NATS_URL=nats://localhost:4222
# Shared secret workers must present in x-worker-token to post callbacks.
# Leave unset only in local/dev; required (else 503) when NODE_ENV=production.
WORKER_TOKEN=
