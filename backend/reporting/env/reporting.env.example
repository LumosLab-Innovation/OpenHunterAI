NODE_ENV=development
SERVICE_NAME=reporting
REPORTING_PORT=4400
DATABASE_URL=postgresql://xhunter:xhunter@localhost:5432/xhunter?schema=public
# Shared secret required on /internal/* callbacks (orchestrator). Required when
# NODE_ENV=production; unset only in local/dev.
WORKER_TOKEN=
