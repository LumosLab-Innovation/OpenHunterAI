name: playwright
language: go
mode: mcp
requires_runtime: false
docker_profile: playwright
internal_base_url: http://playwright-adapter:6140
health_endpoint: /health
required_env:
  - PLAYWRIGHT_MCP_URL
capabilities:
  - browser_crawl
  - route_observation
