name: strix
language: go
mode: http
requires_runtime: true
docker_profile: strix
internal_base_url: http://strix-adapter:6130
health_endpoint: /health
required_env:
  - STRIX_BIN
capabilities:
  - adversarial_reasoning
  - access_control_analysis
